<!-- Optional; Here the user could browse between suggested/random items ->
<?php include "header.phtml" ?>
<?php include "menu.phtml"?>
<!------------------------------>
<!-- PC , USB, MOTHERBOARD, GRAPHICS CARD, ACCESSORIES-->
<div id="sideBar" >
    <form>
        <h1>Filter</h1>
        <input type="radio" name="stock" value="all"><label>&nbsp;All</label><br>
        <input type="radio" name="stock" value="inStock"><label>&nbsp;In-Stock-only</label><br>
        <input type="radio" name="stock" value="outStock"><label>&nbsp;Out-of-Stock-only</label><br><br><br><br>
        <?php
        foreach($categories as $category) {
            echo "<input type=\"checkbox\" name=\"{$category->getName()}\""
                . " value=\"{$category->getName()}\"><label>&nbsp;{$category->getName()}</label><br/>";
        }
        ?>
    </form>
</div>
<div id="ProductsTableDiv">
    <h1>Available Products</h1>
    <table class="with-currency ProductsTable" lang="en-NZ">
        <tr>
            <th>SKU</th>
            <th>Name</th>
            <th>Category</th>
            <th>Cost</th>
            <th>Stock Qty</th>
        </tr>
        <tbody class="productsTableBody">
        <?
            foreach($products as $product ){
                $cost = $product->getCost() / 100;
                echo "
                    <tr>
                        <td>{$product->getStockKeepingUnit()}</td>
                        <td>{$product->getName()}</td>
                        <td>{$product->getCategory()->getName()}</td>
                        <td>{$cost}</td>
                        <td>{$product->getQuantity()}</td>
                    </tr>
                ";
            }
        ?>
        </tbody>
    </table>
</div>
<!------------------------------>
<?php include "footer.phtml" ?>
